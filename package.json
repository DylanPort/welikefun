{
  "name": "welikefun",
  "version": "1.0.0",
  "description": "Fun websites discovery platform",
  "scripts": {
    "build": "npm run minify:css && npm run minify:js && npm run copy",
    "minify:css": "cleancss -o dist/styles.min.css styles.css",
    "minify:js": "terser script.js -o dist/script.min.js -c -m && terser supabase-service.js -o dist/supabase-service.min.js -c -m",
    "copy": "node build.js"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.2",
    "terser": "^5.26.0"
  }
}

